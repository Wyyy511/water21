# WaterPulse V10.2 部署与 Demo

1. 将本压缩包全部文件上传到 GitHub 仓库根目录。
2. Railway 保持 `DEEPSEEK_API_KEY` 环境变量。
3. 部署后 `/api/health` 应显示 `version: 10.2`。
4. 网站“下载 Excel 模板”得到的文件只包含 `采购数据` 和 `填写说明` 两个工作表。
5. 甘蔗主 Demo 使用网页“体验示例”按钮；甜菜和大豆作为补充迁移/多材料验证，建议通过同一空白模板分别填写后上传。
6. 三种材料的示范采购结构均应明确为研究团队设定/模拟输入，不对应真实企业。
