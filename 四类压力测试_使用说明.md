# 四类压力测试使用说明

## 怎样选择

1. 上传企业采购Excel/CSV，或输入并确认采购来源和比例。标准模板直接进入分析；非标准字段按原流程确认。
2. 当前风险结果下方显示四个按钮：主要供应地中断、极端干旱、关键用水期压力升高、未来水环境变化（演示）。
3. 点击任一按钮运行该项比较；完成后仍保留四个按钮，可逐项切换。每次沿用同一份已确认采购数据和当前基准结果，不将前一个压力情景当成新基准。
4. 输入框工具栏的“压力测试”按钮可随时打开菜单，也可直接输入“让我选择压力测试”。尚未提供足够采购信息时，先补充数据。
5. 结果卡片中的“情景数据来源与假设”可展开查看对应来源和限制。

## 四类情景

| 按钮 | 对应程序 | 比较内容 | 边界 |
|---|---|---|---|
| 主要供应地中断 | NodeFailure | 风险贡献最高供应节点中断后的供应损失、库存和替代供应 | 默认完全中断、库存10%，替代容量为原项目参考假设 |
| 极端干旱 | ExtremeDrought | 按登记的历史极端干旱指标重算风险 | 30%供应损失率仍是文献参考设定，不是实际作物减产预测 |
| 关键用水期压力升高 | PeakSeason | 按月度水压力最高三个月的均值替换水压力路径 | 当前未使用已核验作物关键期，按原程序备选规则比较 |
| 未来水环境变化（演示） | AqueductFuture | 按指定年份和路径的WS/SV重算 | 按钮默认2050年BAU路径，原数据为S类演示；并未接入真实未来数据 |

## 缺失怎样处理

缺少节点、关键月份或指定未来组合时停止该项测试，列明缺口，保留当前基准结果和其他压力测试按钮。极端干旱未识别到事件的节点不会被当成0风险汇总为完整组合。新地区、新作物若没有登记数据，仍不可凭旧案例补齐。

当前修改针对原WaterPulse Agent压力测试交互与已有程序调用；此前交接资料没有因为本次修改成为新查询数据库。TEST_ONLY数据不得用于真实结果。

## 修改与验证

服务端统一返回四类按钮；成功、失败、重试解释和重新打开菜单均保留选择入口。修复情景失败后退回基准成功提示的问题。情景工具增加逐节点数据检查，保留选中材料、用户采购权重、来源、假设和基准输入哈希。解释层将情景来源和假设纳入事实材料。两份页面同步更新。原压缩包中四个演示Excel的乱码文件名恢复为中文名称，文件内容不改。

30项相关Python测试通过，覆盖自定义采购CSV上传、四类连续切换、比例与基准一致、缺失月份/节点提示、自然语言情景失败、原案例及上传限制。实际前端按钮渲染函数、事件分发和FormData请求在本地HTTP服务完成四类调用验证；两份页面JavaScript语法检查通过。未完成浏览器视觉渲染验证，未调用真实DeepSeek服务。

本次不修改确定性计算公式或基础CSV/JSON数值。
